@model IEnumerable<Proyecto_Sebet.Models.Career>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameCareer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionCareer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameCareer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescriptionCareer)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCareer">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCareer">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCareer">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
